import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    customGrey: {
      main: '#F0F2EE',
      light: '#F0F0E0',
    },
    customGreen: {
      main: '#128938',
      light: '#4CAF50',
      dark: '#005F00',
    },
  },
})

export default theme
